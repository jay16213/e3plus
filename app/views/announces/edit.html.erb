<%= form_for @announce, :url => {action: 'update', id: @announce } do |f| %>
    <!--include _announce_form.html.erb-->
    <%= render partial: 'form', locals: { f: f } %>
    
    <%= f.submit "修改公告" %>
<% end %>